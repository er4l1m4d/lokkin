{
  "name": "nivora",
  "private": true,
  "version": "0.1.0",
  "description": "Nivora — commitment-based competitive study trials on Nimiq",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend run test",
    "checks": "npm --prefix frontend run lint && npm --prefix frontend run test && npm --prefix frontend run build",
    "install:frontend": "npm --prefix frontend install",
    "api": "python -m uvicorn app.main:app --reload --app-dir backend"
  }
}
