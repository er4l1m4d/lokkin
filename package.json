{
  "name": "qestia",
  "private": true,
  "version": "0.1.0",
  "description": "Qestia — commitment-based competitive study challenges on Nimiq. Where knowing becomes proving.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend run test",
    "checks": "npm --prefix frontend run lint && npm --prefix frontend run test && npm --prefix frontend run build",
    "install:frontend": "npm --prefix frontend install",
    "api": "python -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8000"
  }
}
