# Copy to .env for local development
# true = in-memory mock (no backend needed); false = real FastAPI
VITE_USE_MOCK=true
# Base URL of the FastAPI backend (used when VITE_USE_MOCK=false)
VITE_API_URL=http://localhost:8000
